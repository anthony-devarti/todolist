export default function ToDoItem(){
    //for loop that generates one of these divs for each object in the toDos array
    return (
        <>
            <button>Done!</button>Build App<button>Delete</button>
        </>
    )
}